{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block main %}

<nav class="mealNav new">
    <div class="top">
        {# <a href={{ path('app_recipes_day') }} class="button"> Nos commandes <a> #}
        <a href={{ path('app_calendar_index') }} class="button"> Le calendrier <a>
    </div>
     <div class="top">
        <a href={{ path('app_genre') }} class="button">Créer des genres  <a>
        <a href={{ path('app_ingredients') }} class="button"> Créer des ingrédients <a>
    </div>
    <div class="top">
        <a href={{ path('view_genre') }} class="button"> Gérer les genres <a>
        <a href={{ path('display_ingredients') }} class="button"> Gérer les ingrédients <a>
    </div>
    <div class="top">
        <a href={{ path('app_contact_list') }} class="button"> Voir les messages <a>
    </div>
</nav>
{% endblock %}
